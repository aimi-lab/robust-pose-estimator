### SLAM Baseline Methods

## Elastic Fusion
You need to clone and install elastic fusion as:

    git clone https://github.com/aimi-lab/efusion.git
    cd efusion
    git fetch
    git checkout -t python/python
    ./dependency_build.sh
    ./elasticfusion_build.sh
    
Create a link from 

## ORBSLAM 2
You need to clone and install orbslam2 as:

    ./build_orbslam.sh
    cd Vocabulary
    tar -xzf ORBvoc.txt.tar.gz
    
Create a soft link in your python environment the shared library.

    ln -s  orbslam_python/lib/orbslam2.so /usr/lib/python3.6/dist-packages/orbslam2.so